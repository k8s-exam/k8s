{
  "name": "ckad-exercises-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static CKAD exam-prep site built from the CKAD-exercises repo, styled with Tailwind CSS.",
  "scripts": {
    "build:css": "tailwindcss -i css/input.css -o css/style.css --minify",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
